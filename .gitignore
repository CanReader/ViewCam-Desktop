build/
build-release
bin/
dist/
installer/output/
cmake-build-*/
# Prebuilt FFmpeg LGPL-shared runtime (downloaded, not vendored — see CMakeLists FFmpeg section)
third_party/ffmpeg/
.cache/
compile_commands.json
*.o
*.so
*.user
.idea/
.clangd
.nvim-lsp-settings.json
.vscode/
compile_flags.txt

# Update signing PRIVATE key — NEVER commit (public key + hex are safe to commit)
release/update-signing-key.pem

# Local build secrets / ingest keys. .env.example IS committed as the template.
.env
release/ftp.netrc

# Staged third-party driver installers (VB-CABLE is not redistributable)
tools/VBCABLE/
